resource "aws_connect_quick_connect" "_319_renstar_med_research" {
	instance_id  = var.connect_instance_id
	name         = "319 Renstar Med Research"
	quick_connect_config {
	  quick_connect_type = "PHONE_NUMBER"

        phone_config {
          phone_number = "+13526295800"
        }
	}
	tags = var.tags
}
